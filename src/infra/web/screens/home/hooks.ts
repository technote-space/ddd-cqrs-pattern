import type { ScreenProps } from '$/web/pages';

// eslint-disable-next-line unused-imports/no-unused-vars
export const useHooks = (props: ScreenProps) => {
  // TODO: switch pages

  return {};
};
export type HooksParams = ReturnType<typeof useHooks>;
