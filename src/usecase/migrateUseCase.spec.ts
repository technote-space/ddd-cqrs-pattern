
describe('MigrationUseCase', () => {
  it('存在しないテーブルを新しく1つ作成', () => {

  });

  it('リレーション関係にある存在しないテーブルを作成', () => {

  });

  it('存在するテーブルのIDを使用してリレーション関係にあるテーブルを新しく作成', () => {

  });

  it('すでにすべてのテーブルが存在する場合はテーブルが作成されない', () => {

  });
});
